# AI Update Interval 프로파일링

이 폴더는 P33 / N17 전후에 사용한 초기 AI update interval 프로파일링 문서를 보관한다.

raw CSV 캡처는 repository에서 제거했다. 원본 case 이름과 측정 조건은 `CSV_Evidence_Manifest.md`에 보존한다.

## 문서

| 파일 | 목적 |
| --- | --- |
| `CSV_Evidence_Manifest.md` | 원본 raw CSV case 목록과 측정 조건 기록 |
