Edit target is the supplied real project gameplay screenshot. Preserve its character, enemy, environment, camera, sky, ground and combat VFX unchanged. Overlay a clean Stellar Blade-inspired HUD design preview, not a real runtime claim. No extra characters or replacement environment. Keep exact screenshot aspect ratio. Top center: spaced TARGET, fine densely segmented white square-grid HP 60% full, dim empty cyan grid below labeled AUX —, then three small yellow diamonds two filled one outlined aligned left. Bottom left: compact vertical stack BU —, BE —, HP grid 72% full, SH —; use dense small square cells, white HP, gray hollow unavailable rows. To the LEFT of that stack, two small vertically stacked monochrome consumable vial slots with em dash count, and one small upper auxiliary circle. Bottom right: compact four dim circular skill slots arranged as diamond, original monochrome slash, sweep, downstrike, shield pictograms; tiny unavailable badges, no costs or bound keys. One small inactive auxiliary circle above. Label each TODO group discreetly 'TODO'; no huge explanatory panels or icon samples. White labels with subtle dark stroke must read over bright background. Top left small 'HUD LAYOUT PREVIEW / NOT IN-GAME UI'. Do not add a filled background panel. Center combat remains entirely unobstructed. Font rendering is conceptual and not an exact font specimen.
